# Identity — Spec (synthetic)

**Status:** Clarified

## What changes
Authentication + a list endpoint. Minimal spec so the fixture mirrors a real consumer.

## Functional requirements
- FR-1. A user can authenticate (scn-001).
- FR-2. An authenticated caller can list items (scn-002).
