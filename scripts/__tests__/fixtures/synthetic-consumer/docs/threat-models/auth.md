# Threat model — Auth (synthetic)

STRIDE placeholder so INV-2 (require-human-review ⇒ threat model exists) passes for
the sensitive issue #001. Spoofing / Tampering / Repudiation / Information disclosure /
Denial of service / Elevation of privilege — each addressed by the reference pattern.
