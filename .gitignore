# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build
dist/
build/
*.tsbuildinfo
coverage/

# Env
.env
.env.local
.env.*.local

# Claude Code session/cache (skills/agents/hooks SÍ se versionan)
.claude/cache/
.claude/.local/
.claude/sessions/

# Dry-run artifacts internos (no son parte del framework)
test-run-feature/
test-run-feature-2/

# Ephemeral Ralph (Night Shift) session logs — written by ralph-lib.sh at runtime.
# .planning/ tracks dry-runs/, pr-bodies/, handoff/ deliberately; these logs are NOT
# framework content and must never be committed (they also embed local working paths).
.planning/ralph-sessions/

# Contributor-local comparative analysis (not part of the shipped framework)
Analisis-Comparativo-Frameworks-AI-Development.md
